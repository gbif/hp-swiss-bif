url: "https://swiss-bif.hp.gbif-staging.org/" # the base hostname & protocol for your staging site, e.g. https://hp-example.gbif-staging.org
testSite: true
include: # this include is only to NOT exclude translations, so that they are visible in the staging
  - en/
  - it/
  - de/
  - _posts/en
  - _posts/it
  - _posts/de
